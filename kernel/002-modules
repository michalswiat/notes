# Simple module

## OOT (out of tree)
The simplest kernel module contains init and exit function. The example can be find at ~/my-distr/simple.
There is also a makefile. It is specific for kernel, path to kernel needs to be specified.

## in tree

# e1000 network module
